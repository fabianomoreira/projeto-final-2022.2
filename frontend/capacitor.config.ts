import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ProjetoFinal_JP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
